experiment_project: 'Mitty'
experiment_name: 'Masquerade_finetune'
model_id:    "/home/cloud-user/fga/Mitty/ckpt/Wan2.2-T2V-A14B-Diffusers"
output_root: '/home/cloud-user/fga/Mitty/output'
use_DiffSynth: True
use_drop_text: True
use_lora: True

training:
  resume_path: ~
  batch_size: 1
  max_steps: 19000
  warmup_steps: 10
  learning_rate: 1e-5
  weight_decay: 1e-2
  save_val_interval_steps: 100
  accumulate_grad_batches: 2
  gradient_clip_val: ~
  precision: "bf16-mixed"
  accelerator: "auto"
  strategy: 'auto'
  gradient_checkpointing: True
  benchmark: False

dataset:
  video_root:  '/home/cloud-user/fga/Mitty/dataset/Masquerade/human'
  video_root2: '/home/cloud-user/fga/Mitty/dataset/Masquerade/robot'
  first_root:  '/home/cloud-user/fga/Mitty/dataset/empty'
  is_one2three: True
  height: 224
  width: 416
  sample_n_frames: 81
  fps: 16
  num_workers: 8
  shuffle: True
  drop_last: True
  pin_memory: True
